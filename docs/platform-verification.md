# 跨平台内测验证记录

2026-09-16，本机 Windows。

- 26 项核心测试通过：宠物包读写、非法内容拒绝、仅使用已有动作、自动轮换、手动/自动睡眠、暂停、提醒、专注计时和多屏坐标。
- 独立 Edge 完成 8 组操作验证：试播、暂停、替换照片、擦除撤销、单帧行走拒绝、多帧添加、导出再导入、错误文件保留当前作品。
- 1440、1024、390 像素宽度均无横向溢出；完成截图检查。修正手机入口、标题字体、帮助链接换行与下载状态说明。
- 自托管 Noto Serif SC 标题字体经浏览器确认 loaded；字体许可随源码提供。
- 本机抠图真实返回 960×960 RGBA，透明通道范围 0～255；无关来源 403、损坏图片 422。约 3 秒的结果仅来自本机单张示例，不代表服务器吞吐量。
- Electron 原生 Windows 启动检查确认真实猫咪像素、透明窗口边角、置顶与渲染沙箱。检查短暂显示窗口后退出。
- 网页生产构建通过。Windows ZIP 完整性检查通过，解压后的实际程序也通过原生启动检查。
- 本机启停脚本实际验证：两个服务均停止，重新启动后网站 HTTP 200，抠图服务 ready。
- [首次跨平台构建](https://github.com/flyallz/desktop_pets/actions/runs/35094065402) 完成：Windows x64、Mac arm64、Mac x64 启动检查均通过；Mac Universal 打包通过，输出架构清单随构建保留。
- 上述是云端构建与原生启动验证；未进行朋友的 M 系列/Intel 真机交互验收，未签名或公证。内测发布页区分系统并保留这些限制。

内置浏览器连接因本机运行环境故障未能启动，页面验证改用隔离的 Edge 测试进程，没有读取日常浏览器资料。
独立审阅复核确认其列出的三处修复已解决；这不构成整个商业系统已就绪的证明。


## v0.5.0-beta.2：点击动作与造型

- 34 项核心检查通过，新增旧包默认造型、配饰与分姿势位置往返、非法位置拒绝、可用动作轮换及菜单期间继续计时。
- 7 组新交互检查通过：点击菜单、快捷换姿势与换装、调整后导出再导入、拖动不误触、键盘 Enter/Esc、宽窄屏菜单、单张自定义照片不借用示例动作。另复核原有 8 组制作流程，控制台无错误。
- 1440、1024、390 像素宽度检查无横向溢出；集中查看了菜单、配饰调整及四种姿势的位置。
- Windows 原生检查通过真实鼠标点击打开菜单、保存蓝领结、重新加载后恢复选择，同时保持透明窗口、置顶和沙箱。
- 配饰为可调的几何叠加层，未改写原图；不是 AI 重新生成的整套服装。
- 本版新增原生检查也由三平台构建执行，具体运行结果见 GitHub Actions。Mac 仍未签名、公证，真机使用验收仍待完成。
